---
title: 'Output Mapping'
description: 'Format your transformed documents'
icon: 'table'
---

Output Mapping defines the structure of the output document.

This mapping will change depending on the destination:
- If the destination involves an Excel file, then we will require the column locations.
- If the destination is a table, then we require the column type.


column locations of a schema. The definition of the output mapping will depend on the destination.

If the destination involves an Excel file, then we will require the column locations. If the destination is a table, then we require the column type.
